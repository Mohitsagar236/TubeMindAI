import { useState } from 'react';
import { CheckCircle2, HelpCircle, XCircle } from 'lucide-react';
import { QuizQuestion } from '../types';
export function QuizPage({ questions }: { questions: QuizQuestion[] }) { const [index, setIndex] = useState(0); const [selected, setSelected] = useState<string | null>(null); const q = questions[index]; if (!q) return <div className="flex flex-1 items-center justify-center text-xs text-slate-500">No quiz generated yet.</div>; return <main className="flex-1 overflow-y-auto p-5"><div className="flex items-center justify-between"><p className="text-[10px] font-bold uppercase tracking-widest text-indigo-600">Knowledge check</p><span className="text-[10px] text-slate-400">{index + 1} / {questions.length}</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-slate-200"><div className="h-full bg-indigo-600 transition-all" style={{ width: `${((index + 1) / questions.length) * 100}%` }}/></div><h1 className="mt-5 text-[15px] font-semibold leading-6 text-slate-900">{q.question}</h1><div className="mt-4 space-y-2">{q.options.map((option, i) => { const answered = selected !== null; const correct = option === q.correctAnswer; const chosen = option === selected; return <button key={option} disabled={answered} onClick={() => setSelected(option)} className={`flex w-full items-center gap-3 rounded-xl border p-3 text-left text-xs transition ${answered && correct ? 'border-emerald-300 bg-emerald-50 text-emerald-800' : answered && chosen ? 'border-red-300 bg-red-50 text-red-800' : 'border-slate-200 bg-white hover:border-indigo-300'}`}><span className="grid h-6 w-6 shrink-0 place-items-center rounded-lg bg-slate-100 text-[10px] font-bold">{String.fromCharCode(65+i)}</span>{option}{answered && correct && <CheckCircle2 className="ml-auto" size={16}/>} {answered && chosen && !correct && <XCircle className="ml-auto" size={16}/>}</button>})}</div>{selected && <div className="mt-4 rounded-xl bg-slate-100 p-3 text-[11px] leading-5 text-slate-600"><strong className="block text-slate-800">Explanation</strong>{q.explanation}</div>}<button disabled={!selected} onClick={() => { setIndex((index + 1) % questions.length); setSelected(null); }} className="mt-4 w-full rounded-xl bg-slate-900 py-3 text-xs font-semibold text-white disabled:opacity-30">{index === questions.length - 1 ? 'Start over' : 'Next question'}</button></main>; }
export function QuizEmpty() { return <div className="flex flex-1 flex-col items-center justify-center text-center"><HelpCircle size={32} className="text-indigo-500"/><p className="mt-3 text-sm font-semibold">No quiz generated yet</p></div>; }
