import { FormEvent, useState } from 'react';
import { Check, KeyRound, Server } from 'lucide-react';
import { Settings } from '../types';
export function SettingsPage({ settings, onSave }: { settings: Settings; onSave: (settings: Settings) => Promise<void> }) { const [draft, setDraft] = useState(settings); const [saved, setSaved] = useState(false); const submit = async (e: FormEvent) => { e.preventDefault(); await onSave({ backendUrl: draft.backendUrl.trim().replace(/\/$/, ''), apiKey: draft.apiKey.trim() }); setSaved(true); setTimeout(() => setSaved(false), 1800); }; return <form onSubmit={submit} className="flex-1 overflow-y-auto p-5"><h1 className="text-lg font-bold text-slate-900">Settings</h1><p className="mt-1 text-xs leading-5 text-slate-500">Connect the extension to your TubeMind backend.</p><label className="mt-6 block text-xs font-semibold text-slate-700"><span className="mb-2 flex items-center gap-2"><Server size={14}/>Backend URL</span><input required type="url" value={draft.backendUrl} onChange={e => setDraft({ ...draft, backendUrl: e.target.value })} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-xs outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100" placeholder="http://localhost:8000"/></label><label className="mt-4 block text-xs font-semibold text-slate-700"><span className="mb-2 flex items-center gap-2"><KeyRound size={14}/>OpenAI API key <em className="font-normal text-slate-400">(optional)</em></span><input type="password" autoComplete="off" value={draft.apiKey} onChange={e => setDraft({ ...draft, apiKey: e.target.value })} className="w-full rounded-xl border border-slate-200 bg-white px-3 py-3 text-xs outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100" placeholder="sk-••••••••"/></label><div className="mt-3 rounded-xl bg-amber-50 p-3 text-[10px] leading-4 text-amber-800">For this MVP, the API key is stored only in Chrome local storage and sent to your configured backend. It is never logged by the extension.</div><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-indigo-600 to-blue-600 py-3 text-xs font-semibold text-white shadow-lg shadow-indigo-100">{saved ? <><Check size={15}/>Saved</> : 'Save settings'}</button></form>; }
