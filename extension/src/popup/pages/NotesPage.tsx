import ReactMarkdown from 'react-markdown';
import { BookOpen, Copy } from 'lucide-react';
export function NotesPage({ title, content }: { title: string; content: string }) { return <main className="flex-1 overflow-y-auto p-4"><div className="mb-3 flex items-center justify-between"><div><p className="text-[10px] font-bold uppercase tracking-widest text-indigo-600">Study notes</p><h1 className="text-base font-bold text-slate-900">{title}</h1></div><button onClick={() => void navigator.clipboard.writeText(content)} className="rounded-xl border border-slate-200 bg-white p-2 text-slate-500 hover:text-indigo-600" title="Copy notes"><Copy size={15}/></button></div><article className="markdown rounded-2xl border border-slate-200 bg-white p-4 text-xs leading-5 text-slate-700 shadow-soft"><ReactMarkdown>{content}</ReactMarkdown></article></main>; }
export function NotesEmpty() { return <div className="flex flex-1 flex-col items-center justify-center p-8 text-center"><BookOpen className="text-indigo-500" size={32}/><h2 className="mt-3 text-sm font-semibold">No notes yet</h2><p className="mt-1 text-xs text-slate-500">Return to chat and choose Notes to generate a revision-ready guide.</p></div>; }
