import { useState } from 'react';
import { ChevronLeft, ChevronRight, Layers3, RotateCw } from 'lucide-react';
import { Flashcard } from '../types';
export function FlashcardsPage({ cards }: { cards: Flashcard[] }) { const [index, setIndex] = useState(0); const [flipped, setFlipped] = useState(false); if (!cards.length) return <div className="flex flex-1 flex-col items-center justify-center text-center"><Layers3 size={32} className="text-indigo-500"/><p className="mt-3 text-sm font-semibold">No flashcards generated yet</p></div>; const card = cards[index]; const move = (by: number) => { setIndex((index + by + cards.length) % cards.length); setFlipped(false); }; return <main className="flex flex-1 flex-col p-5"><div className="flex items-center justify-between"><p className="text-[10px] font-bold uppercase tracking-widest text-indigo-600">Flashcards</p><span className="text-[10px] text-slate-400">{index + 1} / {cards.length}</span></div><button onClick={() => setFlipped(!flipped)} className={`mt-5 flex min-h-[280px] flex-1 flex-col items-center justify-center rounded-3xl border p-7 text-center shadow-soft transition ${flipped ? 'border-indigo-200 bg-gradient-to-br from-indigo-600 to-blue-600 text-white' : 'border-slate-200 bg-white text-slate-900'}`}><span className={`text-[9px] font-bold uppercase tracking-[.2em] ${flipped ? 'text-indigo-200' : 'text-slate-400'}`}>{flipped ? 'Answer' : 'Question'}</span><p className="mt-4 text-base font-semibold leading-7">{flipped ? card.back : card.front}</p><span className={`mt-7 flex items-center gap-1 text-[10px] ${flipped ? 'text-indigo-200' : 'text-slate-400'}`}><RotateCw size={12}/>Tap to flip</span></button><div className="mt-5 flex items-center justify-center gap-4"><button onClick={() => move(-1)} className="rounded-xl border border-slate-200 bg-white p-3"><ChevronLeft size={18}/></button><button onClick={() => move(1)} className="rounded-xl bg-slate-900 p-3 text-white"><ChevronRight size={18}/></button></div></main>; }
