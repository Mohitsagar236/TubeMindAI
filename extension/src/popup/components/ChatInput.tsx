import { FormEvent, useState } from 'react';
import { SendHorizonal } from 'lucide-react';
export function ChatInput({ onSend, disabled }: { onSend: (value: string) => void; disabled: boolean }) { const [value, setValue] = useState(''); const submit = (e: FormEvent) => { e.preventDefault(); const clean = value.trim(); if (!clean || disabled) return; onSend(clean); setValue(''); }; return <form onSubmit={submit} className="shrink-0 border-t border-slate-200 bg-white p-3"><div className="flex items-end gap-2 rounded-2xl border border-slate-200 bg-slate-50 p-1.5 pl-3 focus-within:border-indigo-300 focus-within:ring-2 focus-within:ring-indigo-100"><textarea rows={1} value={value} onChange={e => setValue(e.target.value)} onKeyDown={e => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); e.currentTarget.form?.requestSubmit(); } }} placeholder="Ask anything about this video…" className="max-h-20 min-h-[36px] flex-1 resize-none bg-transparent py-2 text-xs text-slate-900 outline-none placeholder:text-slate-400"/><button disabled={disabled || !value.trim()} className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-gradient-to-br from-indigo-600 to-blue-600 text-white shadow-md disabled:opacity-40" aria-label="Send"><SendHorizonal size={16}/></button></div><p className="mt-1.5 text-center text-[9px] text-slate-400">Answers are grounded in the video transcript.</p></form>; }
